import { HeaderContainer } from "./styles";

const Header = () => {
  return (
    <HeaderContainer>
      <h1>
        <strong>My</strong>Twitter
      </h1>
      <button>Perfil</button>
    </HeaderContainer>
  );
};

export default Header;
